import React from 'react'
import { useDeviceSize } from './Responsive'

export const useDefineValue = () => {
    // const { isDesktop, isTablet, isMobile } = useDeviceSize()

    const setValue = (data: any[]) => {
        return true
    }

    return
}
